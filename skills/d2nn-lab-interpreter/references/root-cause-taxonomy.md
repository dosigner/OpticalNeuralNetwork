# Root Cause Taxonomy

- `optimization_limited`: objective, schedule, or convergence likely dominates
- `sampling_limited`: grid or passband likely dominates
- `geometry_limited`: spacing, detector distance, or optical layout likely dominates
- `evidence_insufficient`: available artifacts are too incomplete for a confident cause

Only assign a strong physical label when config and metric evidence both support it.
