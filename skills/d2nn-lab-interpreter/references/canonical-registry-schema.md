# Canonical Registry Schema

Each registry row should include:

- `run_id`
- `run_dir`
- `task_family`
- `metrics`
- `config_present`
- `history_present`
- `notes`

Use `evaluation.json` or `test_metrics.json` as the primary metrics source.
