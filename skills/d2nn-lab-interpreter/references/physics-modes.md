# Physics Modes

- `real-space`: direct propagation layout, spacing and detector distance matter directly
- `fourier/4f`: passband, NA, relay configuration, and spatial-frequency truncation matter directly

If run-local config is missing, state that physical interpretation confidence is low.
