# Lumerical Integration

`d2nn.export.lumerical` provides:
- base simulation creation
- layer object generation from height maps
- merge workflow

When `lumapi` is unavailable, a mock backend writes manifest files for CI/testing.
